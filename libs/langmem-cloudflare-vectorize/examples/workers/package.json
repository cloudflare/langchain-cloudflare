{
  "name": "langmem-cloudflare-vectorize-example",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run pywrangler sync && bash ./scripts/setup_pyodide_deps.sh && npx --yes wrangler dev",
    "deploy": "uv run pywrangler sync && bash ./scripts/setup_pyodide_deps.sh && npx --yes wrangler deploy",
    "start": "uv run pywrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.58.0"
  }
}
