[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "uuid_utils"
version = "0.12.0"
description = "Stub uuid-utils for Pyodide compatibility"
requires-python = ">=3.10"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/uuid_utils"]
